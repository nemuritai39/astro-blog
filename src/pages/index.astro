---
import BaseHead from '../components/BaseHead.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import { SITE_TITLE, SITE_DESCRIPTION } from '../consts';
---

<!doctype html>
<html lang="en">
	<head>
		<BaseHead title={SITE_TITLE} description={SITE_DESCRIPTION} />
		<style>
			main {
				width: 100%;
				max-width: 1200px;
				margin: 0 auto;
				padding: 60px 20px;
			}

			/* 标题区域 */
			.hero-section {
				text-align: center;
				margin-bottom: 80px;
			}
			.hero-section h1 {
				font-family: 'Cinzel', serif;
				font-size: clamp(3rem, 8vw, 5rem);
				letter-spacing: 0.2em;
				background: linear-gradient(to bottom, #fff, #d6b25e);
				-webkit-background-clip: text;
				-webkit-text-fill-color: transparent;
				margin-bottom: 20px;
			}
			.hero-section .mark {
				color: #d6b25e;
				font-size: 1.5rem;
				margin-bottom: 20px;
				display: block;
			}

			/* 祭坛网格布局 */
			.altar-grid {
				display: grid;
				grid-template-columns: repeat(3, 1fr);
				gap: 30px;
			}

			@media (max-width: 900px) {
				.altar-grid {
					grid-template-columns: 1fr;
				}
			}

			/* 通用卡片基础 */
			.temple-card {
				position: relative;
				background: linear-gradient(145deg, rgba(20,20,20,0.9), rgba(10,10,10,0.95));
				border: 1px solid rgba(255, 255, 255, 0.05);
				border-radius: 2px; /* 极小的圆角，更像石碑 */
				padding: 60px 30px;
				text-align: center;
				text-decoration: none;
				transition: all 0.6s cubic-bezier(0.16, 1, 0.3, 1);
				display: flex;
				flex-direction: column;
				align-items: center;
				justify-content: center;
			}

			.temple-card:hover {
				transform: translateY(-8px);
				background: linear-gradient(145deg, rgba(25,25,25,0.95), rgba(15,15,15,1));
			}

			.temple-card h2 {
				font-family: 'Cinzel', serif;
				font-size: 1.7rem;
				letter-spacing: 0.2em;
				margin: 20px 0;
				transition: all 0.4s;
			}

			.temple-card p {
				font-family: 'Cormorant Garamond', serif;
				font-style: italic;
				color: rgba(255, 255, 255, 0.35);
				font-size: 1.05rem;
				line-height: 1.6;
				max-width: 240px;
				transition: color 0.4s;
			}

			.stud {
				width: 6px; height: 6px;
				transform: rotate(45deg);
				margin-bottom: 12px;
				transition: all 0.4s;
				background: rgba(255,255,255,0.2);
			}

			/* --- 不同的金属质感 --- */

			/* 1. Writing - 晨曦金 (Champagne Gold) */
			.card-writing:hover {
				border-color: rgba(247, 231, 206, 0.3);
				box-shadow: 0 10px 40px rgba(0, 0, 0, 0.6);
			}
			.card-writing h2 { color: #f7e7ce; }
			.card-writing .stud { background: #f7e7ce; box-shadow: 0 0 12px rgba(247, 231, 206, 0.5); }
            .card-writing:hover p { color: rgba(247, 231, 206, 0.6); }

			/* 2. Gallery - 纯净白 (Alabaster White) */
			.card-gallery:hover {
				border-color: rgba(255, 255, 255, 0.3);
				box-shadow: 0 10px 40px rgba(0, 0, 0, 0.6);
			}
			.card-gallery h2 { color: #ffffff; }
			.card-gallery .stud { background: #ffffff; box-shadow: 0 0 12px rgba(255, 255, 255, 0.4); }
            .card-gallery:hover p { color: rgba(255, 255, 255, 0.6); }

			/* 3. With You - 古铜金 (Antique Bronze) */
			.card-with-you:hover {
				border-color: rgba(214, 178, 94, 0.3);
				box-shadow: 0 10px 40px rgba(0, 0, 0, 0.6);
			}
			.card-with-you h2 { color: #d6b25e; }
			.card-with-you .stud { background: #d6b25e; box-shadow: 0 0 12px rgba(214, 178, 94, 0.5); }
            .card-with-you:hover p { color: rgba(214, 178, 94, 0.6); }

		</style>
	</head>
	<body>
		<Header />
		<main>
			<section class="hero-section">
				<span class="mark">✠</span>
				<h1>NEMU · ARCHIVE</h1>
			</section>

			<div class="altar-grid">
				<a href="/writing" class="temple-card card-writing">
					<div class="stud"></div>
					<h2>WRITING</h2>
					<p>“ 在墨痕的间隙，捕捉灵魂碎裂的低语。”</p>
				</a>

				<a href="/gallery" class="temple-card card-gallery">
					<div class="stud"></div>
					<h2>GALLERY</h2>
					<p>“ 光影的残像，是时间在这个世界留下的唯一证词。”</p>
				</a>

				<a href="/with-you" class="temple-card card-with-you">
					<div class="stud"></div>
					<h2>WITH YOU</h2>
					<p>“ 跨越禁忌的边界，于虚无中与你同频。”</p>
				</a>
			</div>
		</main>
		<Footer />
	</body>
</html>
