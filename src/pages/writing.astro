---
import BaseHead from '../../components/BaseHead.astro';
import Header from '../../components/Header.astro';
import Footer from '../../components/Footer.astro';
import { SITE_TITLE, SITE_DESCRIPTION } from '../../consts';

const categories = [
	{ id: 'essays', title: '随笔', latin: 'ESSAYS', desc: '记录日常、片段与叙述。', color: 'var(--ivory)' },
	{ id: 'poems', title: '诗歌', latin: 'POEMS', desc: '短句、节奏与情绪。', color: 'var(--silver)' },
	{ id: 'thinking', title: '思考', latin: 'THINKING', desc: '观念、推演与自问自答。', color: 'var(--accent-gold)' },
	{ id: 'goals', title: '目标', latin: 'GOALS', desc: '长期/短期，拆解与回顾。', color: '#b39ddb' },
];
---

<!doctype html>
<html lang="en">
	<head>
		<BaseHead title={`Writing - ${SITE_TITLE}`} description={SITE_DESCRIPTION} />
		<style>
			main {
				max-width: 1000px;
				margin: 0 auto;
				padding: 100px 20px;
				min-height: 80vh;
			}

			.page-header {
				text-align: center;
				margin-bottom: 80px;
			}

			.page-header h1 {
				font-family: 'Cinzel', serif;
				font-size: 3rem;
				letter-spacing: 0.3em;
				color: var(--ivory);
				margin-bottom: 10px;
			}

			.page-header p {
				font-family: 'Cormorant Garamond', serif;
				font-style: italic;
				color: rgba(255, 255, 255, 0.3);
				font-size: 1.1rem;
			}

			/* 档案柜布局 */
			.archive-container {
				display: grid;
				grid-template-columns: repeat(2, 1fr);
				gap: 40px;
			}

			@media (max-width: 768px) {
				.archive-container { grid-template-columns: 1fr; }
				main { padding: 60px 20px; }
			}

			.archive-item {
				position: relative;
				padding: 40px;
				text-decoration: none;
				border-left: 1px solid rgba(255, 255, 255, 0.05);
				background: linear-gradient(to right, rgba(255,255,255,0.02), transparent);
				transition: all 0.5s cubic-bezier(0.16, 1, 0.3, 1);
				overflow: hidden;
			}

			.archive-item::before {
				content: '';
				position: absolute;
				bottom: 0; left: 0; width: 0%; height: 1px;
				background: var(--accent-gold);
				transition: width 0.6s ease;
				opacity: 0.5;
			}

			.archive-item:hover {
				background: linear-gradient(to right, rgba(255,255,255,0.05), transparent);
				transform: translateX(10px);
				border-left-color: var(--accent-gold);
			}

			.archive-item:hover::before {
				width: 100%;
			}

			.latin-tag {
				font-family: 'Cinzel', serif;
				font-size: 0.8rem;
				letter-spacing: 0.4em;
				color: var(--accent-gold);
				margin-bottom: 15px;
				display: block;
				opacity: 0.6;
			}

			.archive-item h2 {
				font-family: 'Cinzel', serif;
				font-size: 1.8rem;
				margin: 10px 0;
				color: #fff;
				transition: 0.3s;
			}

			.archive-item p {
				font-family: 'Cormorant Garamond', serif;
				color: rgba(255, 255, 255, 0.4);
				font-size: 1.1rem;
				line-height: 1.6;
				max-width: 280px;
			}

			/* 每个分类悬停时的色彩光晕 */
			.item-essays:hover h2 { color: var(--ivory); text-shadow: 0 0 15px rgba(240,235,220,0.3); }
			.item-poems:hover h2 { color: var(--silver); text-shadow: 0 0 15px rgba(225,229,233,0.3); }
			.item-thinking:hover h2 { color: var(--accent-gold); text-shadow: 0 0 15px rgba(210,195,160,0.3); }
			.item-goals:hover h2 { color: #b39ddb; text-shadow: 0 0 15px rgba(179,157,219,0.3); }

		</style>
	</head>
	<body>
		<Header />
		<main>
			<section class="page-header">
				<h1>WRITING</h1>
				<p>“ 每一份记录，都是对虚无的一次反击。”</p>
			</section>

			<div class="archive-container">
				{categories.map((cat) => (
					<a href={`/writing/${cat.id}`} class={`archive-item item-${cat.id}`}>
						<span class="latin-tag">{cat.latin}</span>
						<h2>{cat.title}</h2>
						<p>{cat.desc}</p>
					</a>
				))}
			</div>
		</main>
		<Footer />
	</body>
</html>
